/** MIME type for dragging a new component from a palette onto the schema canvas. */
export const SCHEMA_PLAYGROUND_DRAG_ADD_TYPE = 'application/aurora-component';

/** MIME type for reordering / moving an existing node in the schema tree. */
export const SCHEMA_PLAYGROUND_DRAG_MOVE_TYPE = 'application/aurora-move-node';
