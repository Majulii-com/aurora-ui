/**
 * Global UI density / polish.
 * - `aurora` — default Aurora surface language (soft elevation, refined chrome).
 * - `plain` — tighter, flatter legacy-style.
 */
export type AuroraAppearance = 'aurora' | 'plain';
